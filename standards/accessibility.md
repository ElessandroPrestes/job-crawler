# Requisitos e Validações de Acessibilidade

- Interfaces (Swagger UI ou frontend futuro) devem seguir WCAG 2.1 AA (mínimo).
- Outputs de erro de API devem ser claros e em formato estruturado (ex: JSON).
